import { View, Text } from 'react-native';

export default function ProfileLikesTab() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Profile — Likes</Text>
    </View>
  );
}
